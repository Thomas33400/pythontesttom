import pandas
import time

bot_in_action = True
was_in_position = False
startup_info = False

main_order = False
stop_value = 1000

print(f"\nDemarrage du robot\n")

While (bot_in_action == True) :

 try :
   unix = int(time.time())-21*5*60
   reponse = session.query_kline()
   panda_response = pandas.DataFrame(response)
   current_candle_high = panda_resoonse(len-1)

